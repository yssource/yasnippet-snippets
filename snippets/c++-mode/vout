# -*- mode: snippet -*-
# name: vector cout
# key: vout
# group: out
# --
#include <algorithm>
std::for_each(std::cbegin(${1:container}), std::cend($1), [](const auto &e) {std::cout << e << " ";});
std::cout << "\n";$0